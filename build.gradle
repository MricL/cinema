

//// double clic sur build.gradle a gauche. apres avoir rajouté les 3 ligne clicker sur l elephant en haut a gauche.



plugins {
    id 'java'
}

group = 'org.example'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.hibernate:hibernate-core:5.6.9.Final'  ////////////hibermate
    implementation 'org.hibernate:hibernate-entitymanager:5.6.9.Final' /////////hibermate
    implementation 'mysql:mysql-connector-java:8.0.33'
    implementation 'javax.persistence:javax.persistence-api:2.2'   /////////hibermate
    testImplementation platform('org.junit:junit-bom:5.10.0')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

test {
    useJUnitPlatform()
}